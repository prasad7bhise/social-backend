name: CI

on:
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin
          cache: maven

      - name: Build and run unit tests
        run: mvn clean verify -B

      - name: Fail if tests failed
        if: failure()
        run: echo "::error::Tests failed — cannot merge until all tests pass"
